if conf_data.get('CONFIG_CGI')
	srcs += files('cgi.c')
endif
if conf_data.get('CONFIG_DGI')
	srcs += files('dgi.c')
endif
srcs += files('file.c')

if conf_data.get('CONFIG_MAILCAP')
	srcs += files('mailcap.c')
endif
